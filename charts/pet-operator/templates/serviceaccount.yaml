apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pet-operator.fullname" . }}
  labels: {{- include "pet-operator.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
