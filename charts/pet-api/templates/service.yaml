apiVersion: v1
kind: Service
metadata:
  name: {{ include "pet-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "pet-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "pet-api.selectorLabels" . | nindent 4 }}
